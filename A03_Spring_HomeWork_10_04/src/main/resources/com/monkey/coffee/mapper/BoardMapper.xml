<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0 //EN"
"http://mybatis.opg/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.monkey.coffee.mapper.BoardMapper">
	<select id="getBoardList" resultType="BoardVO">
		SELECT * FROM BOARD ORDER BY IDX DESC
	</select>
	<select id="getBoard" resultType="BoardVO">
		SELECT * FROM BOARD WHERE
		idx = #{idx}
	</select>
	<select id="searchIDXs" resultType="int">
		select idx from board where
		creation_userid = #{creation_userid}
	</select> 
	<select id="searchEqualsUserAndBoard" resultType="BoardVO">
		select * from board where 
		creation_userid = #{creation_userid} and 
		idx = #{idx}
	</select>
	<select id="getSeqBoardLastNumber" resultType="int">
		select last_number from USER_SEQUENCES where SEQUENCE_NAME = upper('seq_board')
	</select>
	<insert id="insertBoard" parameterType="BoardVO"> 
		INSERT INTO BOARD(IDX, TITLE, CONTENTS, CREATION_USERID)
		VALUES (seq_board.NEXTVAL, #{title}, #{contents}, #{creation_userid})
	</insert>
	<update id="updateBoard" parameterType="BoardVO">
		UPDATE BOARD SET
		TITLE = #{title},
		CONTENTS = #{contents},
		UPDATE_LAST_DATE = SYSDATE WHERE IDX = #{idx}
	</update>
	<update id="updateBoardHistory" parameterType="int">
		UPDATE BOARD SET
		history_count = history_count + 1
		WHERE IDX = #{idx}
		
	</update>
	<delete id="deleteBoard" parameterType="int">
		DELETE FROM BOARD WHERE 
		IDX = #{idx}
	</delete>
</mapper>